# taking input from users
# for strings
var = readline()
typeof(var)          # will print String
# by default any input in Julia is a String Input

# for numbers
var = parse(Int , readline())
typeof(var)          # will print Int64
# the value inputted from readline() is parsed to Int type 
